# 复制此文件为 .env 并填入你的真实值
# Supabase 项目地址（https://supabase.com → 项目设置）
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# Supabase anon public key（Settings → API）
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# 注意：DeepSeek 的 API Key 不要填在这里！
# 它应配置在 Supabase Edge Function（ai-chat）的环境变量 DEEPSEEK_KEY 中，
# 前端不需要也不应该持有它。放在 VITE_ 前缀的变量会让密钥打进客户端包。
